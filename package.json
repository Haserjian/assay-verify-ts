{
  "name": "assay-verify",
  "version": "0.1.0",
  "description": "Independent TypeScript verifier for Assay proof packs",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:browser": "node scripts/build-browser.mjs",
    "test": "npm run build && node --test dist/verify.test.js"
  },
  "devDependencies": {
    "@types/node": "^22",
    "esbuild": "^0.27.4",
    "typescript": "^5.7"
  },
  "dependencies": {
    "@noble/ed25519": "^2.2",
    "@noble/hashes": "^2.0.1"
  }
}
